services:
  - type: web
    name: core-users-api
    runtime: node
    buildCommand: npm ci && npx tsc && ls -la dist/
    startCommand: node dist/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
